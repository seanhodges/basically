# Acorn Atom dialect — implementation plan

> Generated by the `adding-a-target-system` skill on 2026-06-22. Each stage is a
> medium, single-session task for the coding agent. Run them in order; the
> scaffolding under `src/dialects/atom/` is throwing stubs until filled in. The
> dialect is **not** registered until Stage 3.

## Target summary

- **id / name:** `atom` / `Acorn Atom`
- **CPU / bus pattern:** adapter over the bundled **jsbeeb** package (6502), via a
  new `AtomMachine` in `src/emulator/atom/atomMachine.ts` — a sibling of
  `src/emulator/bbc/bbcMachine.ts`, **not** a reuse of it (the BBC adapter
  hardcodes BBC-only PAGE/MOS/keyboard/video details). Constructs the machine with
  `findModel('Atom-Tape-FP')`.
- **Display size:** 256×192 (the MC6847 VDG text/`CLEAR 4` mode) — this is the
  app default, so `displaySize` can be omitted unless a wider bordered rect is
  preferred (compare the BBC adapter's clipped `896×600`).
- **Image / tape format:** Atom BASIC stores a line as `u16 line number` + ASCII
  body + `0x0D`, with very little keyword tokenising (unlike BBC BASIC's
  single-byte tokens). Program text lives from **`#2900`** by default. Native
  export is an Atom tape/`.atm`-style block image; cassette `.wav` optional.
- **ROM:** copy the Atom ROM set from the jsbeeb package
  (`node_modules/jsbeeb/public/roms/atom/`) into `public/roms/atom/` — the
  `Atom-Tape-FP` model needs `Atom_Kernel.rom`, `Atom_FloatingPoint.rom`,
  `Atom_Basic.rom`. Record origin + licence in `public/roms/ATTRIBUTION.md`. _(Do
  not fabricate ROMs — these already ship with the jsbeeb dependency.)_
- **Reuse:** jsbeeb core + `src/emulator/bbc/bbcMachine.ts` as the adapter
  reference (`typeViaMatrix`, `renderTo`, `setBaseUrl` ROM loading);
  `node_modules/jsbeeb/src/{6847,ppia,utils_atom}.js` for the MC6847 VDG, the
  `AtomPPIA`, and the `ATOM` key-matrix constants; the generic editor seams
  (`src/editor/basicLanguage.ts`, `src/editor/completions.ts`) and keyboard
  seams (`src/keyboard/*`); `src/transfer/wav.ts` for WAV encode.
- **License note:** jsbeeb is **GPL-3.0-or-later** — already a dependency, so no
  new licence exposure (this repo is GPL for the same reason).

## Status legend

✅ shipped · 🔨 in progress · ⬜ planned · ⛔ blocked

| Stage | Title                                  | Status |
| ----- | -------------------------------------- | ------ |
| 1     | Language core                          | ⬜     |
| 2     | Emulator core (jsbeeb Atom adapter)    | ⬜     |
| 3     | Wire-up: keyboard + samples + register | ⬜     |
| 4     | Transfer & tape I/O                    | ⬜     |
| 5     | Polish / optional                      | ⬜     |

---

## Stage 1 — Language core ⬜

Atom BASIC is a genuinely new dialect — it shares **none** of the BBC language
layer (do not import from `src/dialects/bbcmicro/`). Text ↔ program image; no
emulator, no registry change.

- [ ] `keywords.ts` — `KeywordInfo[]` for Atom BASIC (`PRINT`/`P.`, `DO`/`UNTIL`,
      `LINK`, `?`/`!` byte/word indirection, `$` string area, `FOR`/`NEXT`,
      `GOTO`/`GOSUB`, `DIM`, `INPUT`, graphics `PLOT`/`DRAW`/`MOVE`, `CLEAR`,
      `WAIT`…). Drives highlighting + completion.
- [ ] `charset.ts` — `CharsetMapping` for the Atom/MC6847 character codes
      (`toMachine`/`toUnicode`/`glyph`).
- [ ] `language.ts` — `languageSupport()` + `completionSource` via
      `buildBasicLanguage`/`buildCompletionSource`; set `BasicLanguageOptions`
      for Atom quirks (e.g. `hexPrefix: '#'`, dot-abbreviation, indirection
      operators).
- [ ] `tokenizer.ts` / `detokenizer.ts` — text ↔ Atom program image. Collect
      `TokenizeError[]` (1-based line, 0-based column); do not throw. Validate the
      exact `#2900` line-record byte layout against a known-good image booted in
      jsbeeb.
- [ ] image builder (within `tokenizer.ts`/`targets.ts`) — bytes → loadable image
      and parse back.
- [ ] `lint` wired through `tokenize`.
- [ ] tests: tokenizer round-trip, charset, image-builder pointer consistency
      (mirror `src/dialects/bbcmicro/tokenizer.test.ts`).

**Depends on:** the `Dialect` contract only.
**Verify:** `npm test` + `npm run typecheck`.

## Stage 2 — Emulator core (jsbeeb Atom adapter) ⬜

- [ ] `src/emulator/atom/atomMachine.ts` — an `AtomMachine` implementing
      `MachineEmulator`, constructed via `findModel('Atom-Tape-FP')`. Mirror the
      BBC adapter's `setBaseUrl` → `roms/atom/...` loading, but with Atom-specific:
      memory map (program at `#2900`, Atom "top of text" pointer), `renderTo()`
      from the MC6847 framebuffer, and `displayWidth`/`displayHeight`.
- [ ] `loadProgram(image)` — boot the ROM, poke the image at `#2900`, fix the
      top-of-text pointer, then auto-`RUN` by typing `RUN\r` through the
      **AtomPPIA** key matrix (OS-agnostic — reuse the BBC `typeViaMatrix`
      approach remapped to `utils_atom.js` `ATOM` constants in a new
      `src/emulator/atom/keyboard.ts`).
- [ ] `reset` / `runFrame` / `renderTo` / `keyEvent` / `setKey` /
      `releaseAllKeys` / `setSpeed` / `dispose` + `displayWidth`/`displayHeight`.
- [ ] copy Atom ROMs into `public/roms/atom/` + `ATTRIBUTION.md` entry.
- [ ] set `displaySize` on the dialect only if not 256×192.
- [ ] `readVariables` / `readReport` may be omitted at first (ZX80 precedent for a
      dialect without `readVariables`).
- [ ] test: boot the real `public/roms/atom/` ROM, inject a `PRINT` program, run
      frames, assert on the MC6847 display/screen memory (mirror
      `src/emulator/bbc/bbcMachine.test.ts` / `bbcmicro.test.ts`).

**Depends on:** Stage 1 (charset for display, image builder for `loadProgram`).
**Verify:** emulator boot test passes.

## Stage 3 — Wire-up: keyboard + samples + register ⬜

- [ ] `keyboardLayout.ts` — `KeyboardLayout` data; key tokens match the Atom
      adapter's `setKey` (`utils_atom.js` matrix). Use `src/keyboard/templateRows.ts`.
- [ ] `samples/` + `samples.ts` — canonical `hello`/`circles`/`breakout`/`maze`
      ported to Atom BASIC (degrade gracefully; `hello` is the starter).
- [ ] finalize `aiProfile.ts` (system prompt teaching Atom BASIC's rules,
      maxTokens).
- [ ] `index.ts` — assemble the full `Dialect`; wire `createEmulator()` to
      `new AtomMachine()`.
- [ ] **register `atom` in `src/dialects/registry.ts`**.
- [ ] optional `.virtual-keyboard.vk-theme-atom` block in `src/styles.css`.
- [ ] tests: keyboard-layout validation, keyboard matrix (physical+virtual union),
      samples tokenize cleanly.

**Depends on:** Stages 1–2.
**Verify:** `npm run typecheck` + `npm test` + `npm run dev` smoke + `npm run e2e`.

## Stage 4 — Transfer & tape I/O ⬜

- [ ] `targets.ts` — `BuildTarget[]`: native Atom binary export (+ cassette `.wav`
      via `audio`, using `src/transfer/wav.ts`).
- [ ] `audio` — `buildSamples` + `decodeSamples`, `loadInstructions` /
      `saveInstructions` (Atom tape scheme; `*LOAD`/`*SAVE` or `LOAD`/`SAVE`).
- [ ] `binaryImports` — Atom binary format `detokenize()` can read back.
- [ ] tests: cassette encode → decode round-trip.

**Depends on:** Stage 1 (tokenizer/detokenizer, image builder).
**Verify:** audio round-trip test + import/export in the app.

## Stage 5 — Polish / optional ⬜

- [ ] `readVariables()` on the adapter + variable-watcher support.
- [ ] richer build/import targets.
- [ ] Atom BASIC quirks (dot-abbreviation expansion, `?`/`!` indirection niceties).
- [ ] AI-profile accuracy pass.
- [ ] keyboard theming / function-key strip.

**Depends on:** Stage 3.
**Verify:** watcher shows live vars; targeted tests.
</content>
