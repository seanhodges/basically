# Altair 8800 dialect — implementation plan

> Generated by the `adding-a-target-system` skill on 2026-08-02. Each stage is a
> medium, single-session task for the coding agent. Run them in order; the
> scaffolding under `src/dialects/altair8800/` is throwing stubs until filled in.
> The dialect was registered in Stage 3; what is still stubbed is Stage 4's
> `targets.ts`/`audio/` and Stage 5's `memoryMap.ts`, `memoryBlocks.ts`,
> `vars.ts` and `reports.ts`, none of which `index.ts` refers to yet.

## Target summary

- **id / name:** `altair8800` / `Altair 8800`
- **Manufacturer / year:** MITS / 1975 — a new picker group (no sibling machine
  ships), and exactly the lower bound `registry.test.ts` allows
- **Blurb:** `The machine that started it all. Runs Altair 8K BASIC.` (54 chars)
- **BASIC:** Altair 8K BASIC — the 4K set plus strings, `PEEK`/`POKE`,
  `OUT`/`INP`, `DEF FN`, `CSAVE`/`CLOAD` and the full maths library
- **CPU / bus pattern:** in-tree bus over the vendored Z80 core in
  `src/emulator/z80/`, executing Intel 8080 object code
- **Display size:** an 80×24 serial terminal at an 8×16 cell →
  `{ width: 640, height: 384 }`
- **Image / tape format:** no standard container. Cassette audio over the 88-ACR
  board (Kansas City Standard, 300 baud), plain-ASCII paper tape, and the
  tokenized image `CSAVE` wrote
- **ROM:** `public/roms/altair8800.rom` — **user-supplied, does not ship**. See
  _The ROM problem_ below; add the `ATTRIBUTION.md` block explaining the absence
- **Share verb:** `csave` — confirmed in the 8K reserved-word list, unique in
  `SHARE_VERBS`, and consistent with the table's cassette-verb house style
  (`load`, `dload`, `cload`). Fallback `inp`. **Not** `console`, which is
  Extended/Disk BASIC rather than 8K
- **`addressNotation`:** `'dec'` — Altair BASIC's `PEEK`/`POKE` take decimal
- **Reuse:** `src/emulator/z80/`, `src/transfer/wav.ts` and the Acorn 300-baud
  KCS encoders, `src/keyboard/templateRows.ts`, `microsoftVariableErrors` in
  `src/editor/variableLint.ts`, and the linked-line tokenizer pattern already
  written twice in `commodore64/tokenizer.ts` and `trs80/tokenizer.ts`
- **License note:** no new npm dependency; the Z80 core is MIT and already
  vendored

## What makes this machine different

Worth reading before starting a stage, because three assumptions that hold for
every other dialect here do not hold for this one.

**There is no video hardware.** Every other machine renders video RAM. The
Altair renders nothing: BASIC writes bytes to a serial port and a terminal on
the other end displays them. So `emulator/terminal.ts` models the _terminal_,
and `emulator/serial.ts` — the 88-2SIO board — carries the machine's entire
personality, standing in for both the video chip and the key matrix. Two ports
do all of it: a status port BASIC polls, and a data port it reads and writes.

**There are no graphics characters.** Plain 7-bit ASCII, so no `graphics.ts`, no
`graphicsPalette`, no `palette: 'graphics'` editor mode, `SEMIGRAPHIC_CODES`
records `null` rather than a guessed range, and the id must **not** join
`e2e/paletteMachines.ts`. It also means the dialect adds no codepoints to the
bundled font subsets, so `fontCoverage.test.ts` needs nothing from it. This is
the simplest charset in the project — and still has to be total and injective.

**There is no ROM, in two separate senses.** The machine had no firmware at all:
you toggled a bootstrap in on the front panel, or loaded BASIC itself from paper
tape, where it ran from RAM at 0x0000. So the image is _copied into RAM_ rather
than mapped read-only, the memory map has no `rom` region, and a stray `POKE`
really can corrupt the interpreter.

### The ROM problem

Altair 8K BASIC is Microsoft copyright with no redistribution grant. Microsoft's
2025 open-source release was the **6502** BASIC (MIT-licensed), not the 8080
Altair BASIC, so there is no image this project can legally ship. The user
supplies their own at `public/roms/altair8800.rom`.

This is the same situation as the TRS-80's Level II ROM — but there a clean-room
interpreter is the shipped default and the ROM path is the optional accuracy
mode, so the machine works out of the box. Here the positions are reversed:
without a user-supplied image the dialect cannot boot, and
`EmulatorPane.tsx:87-97` throws `Failed to fetch ROM (404)`.

**Stage 3 must therefore treat ROM-absence as a designed state**, with a clear
"supply your own Altair 8K BASIC image" message in the emulator pane, and the
machine must stay constructible with an empty image. If a clean-room interpreter
is ever wanted as the default backend, it slots in as an additive Stage 2b —
mirroring `trs80/interpreter/` — and changes nothing else in this plan.

### Two technical risks

1. **8080-on-Z80 divergence.** The Z80 is binary-compatible with the 8080, which
   is why no new core is needed, but two things differ. The 8080's P flag is
   always parity; the Z80 reuses that bit as P/V, holding _overflow_ after
   arithmetic — and 8K BASIC does branch on parity. And `DAA` after a
   subtraction consults the Z80's N flag, which the 8080 does not have. Decide
   in Stage 2 whether to correct these in the adapter or accept the divergence,
   and record which. **Fix it in the machine adapter, never in the vendored
   core** — it is shared with six shipped machines. **Stage 1 confirmed the
   parity half is fatal, not cosmetic, and what fixes it — see _Two things Stage
   2 must act on_ below.**
2. **Derive, don't guess.** The keyword token bytes, the program base, the
   88-2SIO port numbers and the polarity of its status bits (the ready flags are
   active-low), and `programRamBytes` must each come from a primary source — the
   MITS _Altair BASIC Reference Manual_, the 88-2SIO board manual, or a
   disassembly — and be cited where declared. A wrong token byte produces a
   program that tokenizes cleanly and then fails on real hardware; a wrong status
   polarity produces a machine that boots and then ignores every keystroke.

## Status legend

✅ shipped · 🔨 in progress · ⬜ planned · ⛔ blocked

| Stage | Title                                  | Status |
| ----- | -------------------------------------- | ------ |
| 1     | Language core                          | ✅     |
| 2     | Emulator core                          | ✅     |
| 3     | Wire-up: keyboard + samples + register | ✅     |
| 4     | Transfer & tape I/O                    | ⬜     |
| 5     | Memory map & runtime introspection     | ⬜     |
| 6     | Docs & polish                          | ⬜     |

---

## Stage 1 — Language core ✅

Text ↔ tokenized program bytes; no emulator, no registry change.

- [x] `addresses.ts` — the program base, the interpreter's RAM origin, the
      2SIO ports; each cited to its source
- [x] `keywords.ts` — the 8K `KeywordInfo[]` table (token byte, kind, signature,
      doc), derived from a disassembly or the MITS manual
- [x] `charset.ts` — printable ASCII as itself, control codes and the unused
      0x80–0xFF range as `{0xNN}` escapes; total and injective across all 256
      bytes
- [x] `language.ts` — `languageSupport()` + `completionSource`. Confirm
      `crunched` against the interpreter's CRUNCH routine; `suffixChars` is `$`
      only (8K predates Level II's `%`/`!`/`#` tags); `graphicsEscapes: false`;
      no `hexPrefix`/`binaryPrefix` (`&H` is Disk BASIC)
- [x] `constructsByDialect.altair8800` in `src/editor/constructs.ts`
- [x] `altair8800VariableErrors` in `src/editor/variableLint.ts`, over the
      Microsoft-family helper — 8K names are significant to two characters
- [x] `tokenizer.ts` / `detokenizer.ts` — linked-line layout
      (`[u16 LE link][u16 LE line][body][0x00]`, zero link terminates);
      collect `TokenizeError[]`, never throw
- [x] `basicImage.ts` — tokenized bytes → loadable image (link chain + the
      text/variable pointers), and parse back
- [x] `detokenizeWithReport` for import-fidelity warnings
- [x] tests: tokenizer round-trip, charset totality/injectivity, image-builder
      pointer consistency

**Depends on:** the `Dialect` contract only.
**Verify:** `npm test` + `npm run typecheck`.

### What Stage 1 derived, and how

Everything in `addresses.ts` and `keywords.ts` was read off the MITS 8K BASIC
4.0 object tape itself (`ALTAIR BASIC REV. 4.0 / [EIGHT-K VERSION] / COPYRIGHT
1976 BY MITS INC.`, 8192 bytes, md5 `97eead711723295e9ce4f52b300002cf` — the
image SIMH's AltairZ80 software collection ships as `8kbas.bin`, and the image a
user has to supply at `public/roms/altair8800.rom`). Two methods, and every
value was checked both ways where both applied:

1. **Statically**, from the image. The reserved-word table sits at 0x0073–0x0159
   with a 0x80 end marker: each entry is its spelling with bit 7 set on the
   _first_ character, entries run back to back from END = 0x80 to MID$ = 0xC5,
   70 words with no gaps.
2. **Dynamically**, by booting the image on the vendored Z80 core with a stub
   2SIO, typing programs at its console and reading the stored program text back
   out of memory. `TXTTAB` = 0x1939 (trig retained; 0x1877 if `WANT
SIN-COS-TAN-ATN?` is answered `N`), and the pointer words are `TXTTAB` 0x01D6,
   `VARTAB` 0x024B, `ARYTAB` 0x024D, `STREND` 0x024F.

The tokenizer was then **differentially tested** against that running
interpreter: 33 varied source lines typed at its console produce byte-identical
program text to `tokenizeProgram`, and its `LIST` output matches
`detokenizeProgram` exactly. That harness is not committed (it needs the
copyright image), but it is what the console-derived byte expectations in
`tokenizer.test.ts` are transcribed from.

Behaviour worth knowing before Stages 2–6, all console-confirmed:

- **Crunching matches at each character and does not skip spaces.** `FORI=1TO10`
  tokenizes, but `PR INT 1` keeps `P`,`R` and then tokenizes the `INT`.
- **a–z folds to A–Z outside literals, before crunching** — so `abc=1` stores
  `41 42 43` and `def$` becomes the DEF token plus `$`. Exactly a–z: 0x60 and
  0x7B–0x7E are stored as typed, and strings, REM and DATA are folded not at all.
- **`?` is the only synonym.** It enters as PRINT (0x96) and LISTs back as
  `PRINT`. There is no `'` comment form and no `↑`/`^` split — `^` _is_ the power
  operator (0xA8).
- **Highest line number is 65529**; 65530 answers `?SN ERROR`. The line-number
  scan ignores spaces, so `20 456789…` reads as line 20456789 and is rejected.
- **Bad statements are stored, not rejected** — `10 XYZZY` stores five plain
  characters and fails only on RUN — which is why statement-shape lint is
  `fatal: false`.
- **`%`, `!` and `#` are not type tags.** `10 X%=1` stores and then fails on RUN,
  so `suffixChars` is `$` alone.
- **0x00 cannot appear in a line body** (it is the record terminator), so a
  `{0x00}` escape is a fatal tokenize error rather than a silent truncation.
- The console line buffer holds 72 characters. That is an editor limit, not a
  storage one, so the tokenizer does not enforce it.

### Two things Stage 2 must act on

1. **The 8080 parity flag genuinely blocks booting.** Risk 1 below is not
   theoretical: 8K BASIC's floating-point code runs `SBB A` and then branches on
   `JPO`. On the 8080 `SBB A` leaves P = parity of the result (always even, so
   P set); on the Z80 `SBC A,A` leaves P/V = overflow (clear), so the jump is
   taken and the interpreter spins forever in FP — it prints the cold-start
   dialogue and then never reaches `BYTES FREE`. Fixing it in the machine
   adapter is enough: after each instruction that the 8080 defines P for
   (`ADD/ADC/SUB/SBB/ANA/XRA/ORA/CMP` and their immediate forms, `INR`/`DCR`,
   `DAA`), overwrite the core's P flag with the parity of the result. That is
   what the Stage 1 derivation harness did, and with it the image boots, runs and
   LISTs correctly. **Do not touch the vendored core** — six shipped machines
   share it.
2. **The console is the 88-2SIO at 0x10/0x11, with _active-high_ flags.** The
   image ships wired for the 88-SIO at 0x00/0x01 (active-low: `ANI 01 / JNZ`)
   but patches its own driver at cold start from the sense switches read at port
   0xFF. With the high nibble 0 it rewrites the routines at 0x0547/0x0556 to
   `IN 10 / ANI 02 / JZ` (transmit) and `IN 10 / ANI 01 / JZ` (receive) — the
   6850 ACIA's active-high TDRE and RDRF. So `emulator/serial.ts` must model
   active-high bits and the machine must answer port 0xFF with 0x08, or BASIC
   drives the wrong ports and nothing appears.

## Stage 2 — Emulator core ✅

- [x] `emulator/memory.ts` — flat 64K S-100 bus; `loadInterpreter` copies the
      BASIC image into RAM (no ROM mapping)
- [x] `emulator/serial.ts` — the 88-2SIO: status port, data port in/out, input
      queue
- [x] `emulator/terminal.ts` — 80×24 grid, CR/LF/backspace/bell, scroll on
      overflow, `renderTo`
- [x] `emulator/keyboard.ts` — key tokens and DOM events → queued ASCII bytes,
      including CTRL-C (0x03)
- [x] `emulator/altairMachine.ts` — `MachineEmulator` over the vendored Z80 core
      at `CYCLES_PER_FRAME` (40,000 = 2 MHz ÷ 50); resolve the 8080 flag question
- [x] stays constructible with an empty ROM image
- [x] `public/roms/ATTRIBUTION.md` block recording why no image ships
- [x] test: boot the image, assert the sign-on banner lands in the terminal grid;
      skip (don't fail) when the user-supplied ROM is absent

**Depends on:** Stage 1 (charset for the terminal, image builder for
`loadProgram`).
**Verify:** the boot test passes with a supplied ROM, and the suite stays green
without one.

### What Stage 2 decided

**The 8080 flags are corrected, in the adapter.** Risk 1 is settled the way
Stage 1 said it had to be: after every instruction where the 8080 defines P as
the parity of the result and the Z80 does not — `ADD`/`ADC`/`SUB`/`SBB`/`CMP`
and their immediate forms, `INR`/`DCR`, `DAA` — `Altair8800Machine.step`
overwrites the core's P flag with that parity. `ANA`/`XRA`/`ORA` are left alone
because the Z80 already sets P to parity for its logic instructions, so there is
nothing to correct and no state read to pay for. `DAA` additionally has the N
flag cleared before it runs, which makes the Z80's DAA adjust upwards the way
the 8080's always did; nothing on an 8080 can observe N, so clearing it is free.
The vendored core is untouched.

What is deliberately **not** corrected, and should stay recorded rather than be
quietly discovered later: the half-carry a Z80 `SUB` leaves is a borrow where
the 8080's AC is its complement, so a `DAA` immediately after a subtraction can
still differ in its bottom digit; and `ANA` sets H differently on the two CPUs.
8K BASIC's arithmetic is binary floating point and executes no `DAA` at all, and
nothing reads AC.

**The suite tests the emulator without the copyright image.** Because the
Altair loaded BASIC into RAM rather than running firmware, a hand-assembled
8080 program is as legitimate a boot image as the interpreter — so
`altairMachine.test.ts` runs a dozen of them to cover the bus, the 2SIO's
polarity, the sense-switch port, the terminal, CTRL-C and each flag correction
(every flag case fails if the fix is removed). The four cases that need the real
interpreter skip when `public/roms/altair8800.rom` is absent.

**Two things Stage 3 inherits.** `bootToReady()` is public: left to itself the
machine prints `MEMORY SIZE?` and waits, which is authentic and typeable, so the
pane has to decide whether to open at the dialogue or at the OK prompt. And
`emulator/keyboard.ts` now owns the token vocabulary the ASR-33 rows must emit —
`altair8800KeyTokens()` and `tokenToByte()` are exported for the layout test to
check against.

## Stage 3 — Wire-up: keyboard + samples + register ✅

- [x] `keyboardLayout.ts` — the ASR-33 rows; base + SHIFT layers only, CTRL as a
      real modifier, upper case only, no graphics palette
- [x] `samples/` + `samples.ts` — the canonical set ported to 8K BASIC (see
      _Samples_ below), via the `authoring-dialect-samples` skill
- [x] finalize `aiProfile.ts` — see the "what to unlearn" notes in the stub
- [x] `index.ts` — real `programRamBytes`; ROM-absent handling
- [x] **the five registry-coupled edits, all in this one change:**
  - [x] `src/dialects/registry.ts` — import + array entry
  - [x] `src/player/routes.ts` — `{ verb: 'csave', dialectId: 'altair8800' }`
  - [x] `src/dialects/registry.test.ts` — `expectedNotation.altair8800 = 'dec'`
  - [x] `src/dialects/charsetProbes.ts` — a `CHARSET_PROBES` entry
  - [x] `src/dialects/semigraphicsAudit.ts` — `SEMIGRAPHIC_CODES.altair8800` (`[]`,
        not `null` — see below)
- [x] optional `vk-theme-altair8800` block in `src/keyboard/VirtualKeyboard.css`
- [x] tests: keyboard-layout validation, samples tokenize cleanly

**Why the five must land together.** Each of those tests asserts exact coverage
of the **registered** set, so adding any of them before registration fails
`npm test`, and registering without them fails it too. `charsetProbes.ts` is the
easy one to miss — it is not in the skill's seam table.

**Depends on:** Stages 1–2.
**Verify:** `npm run typecheck` + `npm test` + `npm run dev` smoke +
`npm run e2e:chromium -- e2e/<capability>` for the capabilities touched.

### It was nine registry-coupled edits, not five

The five above are the ones the skill's seam table knows about. Registering the
dialect and running `npm test` turned up four more, each guarded by a test that
sweeps the **registered** set, so all nine had to land in this one change:

6. **`src/reference/altair8800.ts` + `src/reference/escapes/altair8800.ts`** —
   the keyword and escape _data_. `keyword-crosscheck.test.ts` and
   `escape-crosscheck.test.ts` throw at collection time for a registered dialect
   with no page, and the assistant's system prompt is composed from them
   (`machineReference.ts`), so this could not wait for Stage 6. Both were seeded
   with `npm run gen:reference` / `npm run gen:escapes` and hand-enriched. Stage
   6 still owns the _prose_: the hardware / escape-codes / file-formats
   sub-pages and the sidebar wiring.
7. **`src/reference/machines.ts` + `facts.ts` + `domain-guidance.ts`** — the
   porting guide's per-machine list, hardware facts and 13 capability cells.
   `machines-crosscheck` and `facts-crosscheck` require one entry per registered
   machine; `domain-guidance-crosscheck` derives the required cells from the
   real diff, so every domain some other machine can lose into the Altair needs
   "what to do instead".
8. **`src/dialects/glyphSources.ts`** — `GLYPH_SOURCES` and `ADDRESS_SIGIL` both
   assert exact registry coverage. The Altair's entry is `[]` — the only empty
   one — because its shapes belong to whatever terminal is plugged in, and
   `glyphSources.test.ts` now pins that emptiness as a deliberate claim.
9. **`machineArtIds.ts` + `machineArt.tsx`** — the portrait is _not_ optional
   after all: `machineArt.test.ts` requires one per registered machine. Drawn as
   the front panel (lamps over paddle switches), which is the machine's whole
   silhouette.

Two more seams needed no new data but did need a change:
`docs/reference/altair8800.md` had to exist (`docsTopic.test.ts` guards against
a 404 from the docs button), and `DIALECTS_WITHOUT_VARIABLE_READBACK` /
`DIALECTS_WITHOUT_RUNTIME_REPORT` in `src/ai/machineObservability.ts` name the
Altair until Stage 5 gives it `readVariables`/`readReport`.

### What Stage 3 decided

**ROM-absence is a first-class state, carried by a new seam field.** The plan
asked for "a clear supply-your-own message"; getting one meant declaring
`romBytes: 8192` so Settings ▸ Emulator offers the upload at all — and that in
turn made the settings page offer to "restore the bundled ROM" that never
existed. So `Dialect.romBundled?: boolean` was added: `false` says the URL is
where the _user's_ image goes. The emulator pane, the settings copy and the
upload label all read it. `EmulatorPane.fetchRom` also grew a size check against
`romBytes`, because a missing image does **not** reliably 404 — a SPA host (the
Vite dev server included) answers with `index.html` and a 200, which otherwise
reached the machine as an 8080 program and failed deep inside `bootToReady`.

**A machine with no ROM is not offered in the picker.** Registering the Altair
put a row in the machine picker that could only lead to an error, so both
pickers (New project, and the toolbar's target switcher) now filter the registry
through `src/app/machineAvailability.ts`. A machine is offered when it loads its
own ROM set (no `romBytes`), when the user has installed an image of the right
size, or when its bundled image really is there — probed once per page through
the shared `fetchRom` cache, so the probe _is_ the emulator's later download.
The machine the document is already on is always kept, since a share link or a
saved project can reach one. The filter is general rather than Altair-specific:
delete any removable ROM from a build and that machine disappears too, which is
what `public/roms/ATTRIBUTION.md` has always said is allowed.

**Consequence worth knowing:** in a stock build the Altair is now unreachable
from the UI, and Settings ▸ Emulator only offers a ROM upload for the _current_
machine — so the supported route is a self-hosted drop-in at
`public/roms/altair8800.rom`. Giving Settings a "machines needing a ROM" section
would close that loop, and is not in this stage.

**The machine gained `readScreenText`.** Every registered machine must implement
it (`screenReadable.test.ts`), and here it is the easiest one in the project:
the terminal grid already _is_ text. Two test helpers that read a dialect's ROM
off disk (`screenReadable.test.ts`, `machineObservability.test.ts`) now tolerate
an absent file rather than throwing ENOENT.

**The keyboard is an ASR-33, and its SHIFT is a bit-4 flip.** Every shifted
legend in `keyboardLayout.ts` is checked against `tokenToByte` by
`keyboardLayout.test.ts`, in both directions, so `SHIFT-K = [`, `SHIFT-P = @`
and `SHIFT-0 = space` cannot drift from what the machine receives. The layout
reaches exactly the teletype's 64-character set, 0x20–0x5F, and the test pins
that too. LINE FEED / RUB OUT / ALT MODE sit in the top strip as function keys.

**Breakout survived, turn-based.** 8K BASIC has no `INKEY$`, and polling the
2SIO with `INP` does _not_ work around it: BASIC reads the same port looking for
CTRL-C between every statement and takes the character first. So `breakout` and
`maze` read a whole line with `INPUT` — one keystroke is one frame, which is
what the plan's "playable at the terminal's scroll rate" amounts to. The ZX80
and Atom drop breakout entirely for the same want of a key read; here it degrades
rather than disappears.

**`SEMIGRAPHIC_CODES` records `[]`, not `null`.** The plan said `null`, whose
declared meaning is "nobody has read the hardware yet" — and the generated audit
in `docs/contributing/semigraphics-support.md` says exactly that of a null
entry. It is the wrong claim here: the Altair has been read, and it has no
graphics codes because it has no character generator. An empty array is the
opposite claim, and `gen-semigraphics-audit.mts` now renders the two
differently.

**The visual samples use numeric grids, not string arrays.** 8K BASIC allocates
50 bytes of string space until a program says `CLEAR n`, and a 48×16 character
grid held as strings would exhaust it. `circles` and `kaleido` fill a numeric
array and print it a cell at a time, assigning the character from a literal
(`C$="*"`), which points into program text and costs no string space at all.
The vertical axis is halved because a terminal cell is 8×16.

## Stage 4 — Transfer & tape I/O ⬜

- [ ] `targets.ts` — cassette `.wav`, plain-ASCII paper tape, tokenized image
- [ ] `audio/cassetteEncoder.ts` / `cassetteDecoder.ts` — 300-baud Kansas City
      Standard. Start from the Acorn CFS/Atom encoders, **not** the Sinclair
      pulse scheme
- [ ] `audio.loadInstructions` / `saveInstructions` — `CLOAD`/`CSAVE`. If the
      ACR's byte stream carries no name field, say so rather than inventing a
      header
- [ ] `binaryImports` — the formats `detokenize()` reads back
- [ ] test: cassette encode → decode round-trip
      (`src/dialects/cassetteRoundTrip.test.ts` enforces this for any dialect
      offering `decodeSamples`)

**Depends on:** Stage 1.
**Verify:** audio round-trip test + import/export in the app.

## Stage 5 — Memory map & runtime introspection ⬜

- [ ] `memoryMap.ts` — contiguous ascending regions covering the whole space.
      **No `rom` region** (the interpreter is RAM-resident `system`) and **no
      `screen`/`attributes` regions** (there is no display memory); omit those
      kinds rather than inventing regions for them
- [ ] `memoryBlocks.ts` — `cpu: 'z80'` with a comment explaining that the
      machine runs 8080 code and 8080 assembly is a strict subset (the field is
      typed `'z80' | '6502'`; do not widen the union). Valid range sits above the
      interpreter and program, below the top of fitted RAM
- [ ] `vars.ts` → `readVariables()` — walk the VARTAB/ARYTAB table as the C64 and
      TRS-80 do; omitting it is acceptable if the pointers cannot be pinned down
      (ZX80 and Atom precedent)
- [ ] `reports.ts` → `readReport()` — Altair BASIC _prints_ its errors, so scan
      the terminal grid (closest to `src/emulator/c64/reports.ts`); return null
      rather than a false OK
- [ ] `readMemoryStats()` + memory-activity hooks
- [ ] tests: memory-map layout, block round-trip

**Depends on:** Stages 2–3.
**Verify:** memory-map + blocks tests; the watcher shows live variables.

## Stage 6 — Docs & polish ⬜

- [ ] reference docs — run the **`dialect-reference-docs`** sub-skill, which owns
      the scaffold commands, the page templates, the sidebar/index wiring and the
      crosscheck tests
- [ ] roadmap status row in `docs/contributing/dialect-roadmap.md`
- [ ] debugger hooks (`debuggable`, `currentLine`, `debugStep`)
- [ ] optional machine portrait in `src/components/machineArtIds.ts` +
      `machineArt.tsx` (omit and the picker falls back to `'generic'`)
- [ ] AI-profile accuracy pass; keyboard theming
- [ ] no joystick and no sound — the machine has neither, so `joystickModes` and
      `readAudio` stay absent

**Depends on:** Stage 3.
**Verify:** `npm run docs:dev` renders the new pages; targeted tests; full
`npm run lint` + `npm run format:check` before finishing.

---

## Samples

The canonical set, in the canonical order, ported to Altair 8K BASIC. This
machine forces the hardest graceful-degradation call in the project: with no
graphics, no cursor addressing and no screen to clear, `PRINT` to a scrolling
terminal is the entire repertoire. The visual samples therefore plot into a
character grid held in a string array and print the grid.

| `name`         | `title`        | Note                                                                         |
| -------------- | -------------- | ---------------------------------------------------------------------------- |
| `hello.bas`    | `Hello world`  | The starter shown for a fresh document.                                      |
| `circles.bas`  | `Circles`      | Keep the Pitteway recurrence the other dialects use.                         |
| `breakout.bas` | `Breakout`     | Whole-frame redraws, one `INPUT` per frame — there is no key-at-a-time read. |
| `maze.bas`     | `Maze`         | The same fixed wall map, printed as rows of characters.                      |
| `kaleido.bas`  | `Kaleidoscope` | Machine-code block **dropped** — no `USR` block convention here yet, and     |
|                |                | the TRS-80 sets the four-sample precedent.                                   |

## Not needed for this machine

Recorded so nobody spends a stage looking for them: no `graphics.ts`, no
`graphicsPalette`, no `e2e/paletteMachines.ts` entry, no `GLYPH_SOURCES` entry
(`glyphSources.ts` degrades via `?? []`), no font-subset re-cut, no new `e2e/`
folder (that layout maps capabilities, not dialects), no `joystickModes`, and no
`readAudio`.
