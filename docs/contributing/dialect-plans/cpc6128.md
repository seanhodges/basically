# Amstrad CPC 6128 dialect — implementation plan

> Generated by the `adding-a-target-system` skill on 2026-07-22. A
> **delegation plan** (the `bbcmaster` / `zxspectrum128` pattern) over the
> CPC 464 dialect planned in [cpc464.md](./cpc464.md): the two machines share
> the Locomotive BASIC language layer, the `src/emulator/cpc/` machine and one
> `docs/reference/cpc` page. Stages here are deltas — each depends on the
> corresponding cpc464 stage having landed. The dialect is **not** registered
> until Stage C.

## Target summary

- **id / name:** `cpc6128` / `CPC 6128`
- **CPU / bus pattern:** **delegation over `cpc464`** — imports the language
  layer (with the BASIC 1.1 keyword variant), keyboard and samples from
  `src/dialects/cpc464/`, and instantiates the shared machine as
  `CpcMachine('6128')`. Owns only: the 1.1 keyword selection, its emulator
  model config (128K RAM banking + BASIC 1.1 ROM), `memoryMap.ts`,
  `aiProfile.ts`, its metadata, and its sysvar address table.
- **What actually differs from the 464:**
  - **Locomotive BASIC 1.1** instead of 1.0 — adds `CLEAR INPUT`,
    `COPYCHR$`, `CURSOR`, `DEC$`, `DERR`, `FILL`, `FRAME`,
    `GRAPHICS PAPER`, `GRAPHICS PEN`, `MASK`, `ON BREAK CONT`. Same token
    scheme and line format; the additions are carried behind the
    `locoKeywords('basic11')` variant seam built in cpc464 Stage 1.
  - **128K RAM** — a second 64K bank set switched through the Gate
    Array/PAL RAM-configuration port (`&7Fxx`, `0b11…` configurations
    mapping 16K banks over the base 64K). BASIC itself doesn't use the
    extra RAM (it's for CP/M and bank-switching software), so the memory
    seam models the configurations without changing the BASIC experience.
  - **BASIC/firmware workspace addresses** differ between 1.0 and 1.1 —
    supplied as this dialect's sysvar table, not new code (cpc464 Stage 5
    keys its readers by variant).
  - Grey keyboard styling (same 10×8 matrix and legends layout).
- **Deliberately deferred:** AMSDOS + the µPD765 FDC (disc drive). The 6128
  boots and runs BASIC fine without the AMSDOS ROM; shipping it without an
  FDC would hang disc commands. Ship tape-only first (like the 464), and
  treat AMSDOS/FDC + `.dsk` import as a follow-up (Stage D note). This also
  means `programRamBytes` should be set from what the shipped configuration
  actually reports at boot (`PRINT FRE(0)`): 42619 without AMSDOS, 42249
  once AMSDOS is added — don't hard-code the real machine's 42249 while the
  emulator boots without AMSDOS. **Confirmed:** AMSDOS costs exactly the 370
  bytes between the two figures, and BASIC 1.1 leaves a program the same room
  1.0 does, so 42619 (the 464's constant) is right and stays.
- **Display size:** identical to cpc464 (`640×400`, Modes 0/1/2).
- **Image / tape format:** identical to cpc464 (tokenized program +
  AMSDOS-headered `.bas`, `.cdt`, cassette WAV). On a real 6128 tape I/O
  needs `|TAPE` first — reflect that in `loadInstructions` /
  `saveInstructions`.
- **ROM:** shipped as a **single combined 32K `public/roms/cpc/cpc6128.rom`**
  (16K OS 2.x lower + 16K Locomotive BASIC 1.1 upper) — _a deviation from this
  plan's original two-file `cpc6128_os.rom` + `cpc6128_basic.rom`_. The 464
  ultimately shipped one combined `cpc464.rom`, which is what `CpcMemory`'s
  constructor and the single-`romUrl` fetch path expect, so the 6128 follows the
  shipped convention. Copyright © 1985 Amstrad plc / Locomotive Software,
  distributed under the same Amstrad emulator permission as the 464 ROM; the
  `public/roms/ATTRIBUTION.md` CPC block is extended to cover both images with
  their own CRCs. _Do not commit a fabricated ROM._
- **Share verb:** `fill` — `FILL` is a BASIC **1.1-only** keyword, mirroring
  how `play` marks the Spectrum 128 (verb added only at Stage C
  registration; bijection test-enforced).
- **Docs:** `docsReference: 'cpc'` — shares the cpc464 Stage 6 page, where
  1.1-only entries carry a tag.
- **Variable lint:** none, same as cpc464 (fully significant names).

## Status legend

✅ shipped · 🔨 in progress · ⬜ planned · ⛔ blocked

| Stage | Title                                    | Status |
| ----- | ---------------------------------------- | ------ |
| A     | Language delta (BASIC 1.1)               | ✅     |
| B     | Machine variant (128K banking + 1.1 ROM) | ✅     |
| C     | Wire-up: keyboard theme + register       | ✅     |
| D     | Memory map, introspection & docs deltas  | ✅     |

---

## Stage A — Language delta (BASIC 1.1) ✅

- [x] `keywords.ts` — `export const cpc6128Keywords = locoKeywords('basic11')`
      (the variant seam from cpc464 Stage 1), with signatures/docs for the
      eleven 1.1 additions
- [x] ~~`tokenizer.ts` / `detokenizer.ts` — thin re-exports~~ — **not needed.**
      `bbcmaster` passes its variant inline from `index.ts` rather than adding
      wrapper files, and that is what shipped: `tokenizeProgram(src,
'basic11')` and `importCpcImage(image, 'basic11')` are called directly
      from the dialect
- [x] `language.ts` — its own completion source and language support over
      `cpc6128Keywords`; the charset is imported from cpc464 unchanged (no
      `charset.ts` re-export file needed)
- [x] **unplanned:** three cpc464 modules had `'basic10'` hard-coded rather
      than parameterised — `importCpcImage`, `buildCassetteSamples` and
      `targets.ts`'s `programImage`. Each gained the same optional `variant`
      parameter the tokenizer/detokenizer already had, and `targets.ts` became
      a `locoBuildTargets(dialectId, variant)` factory so the two machines get
      distinct target ids
- [x] `constructsByDialect.cpc6128` in `src/editor/constructs.ts` — reuse the
      `cpc464` array (same statement shapes)
- [x] tests: 1.1 keywords tokenize on cpc6128 and are rejected (with a clear
      error) by cpc464; round-trip parity with the base dialect

**Depends on:** cpc464 Stage 1.
**Verify:** `npm test` + `npm run typecheck`.

## Stage B — Machine variant (128K banking + 1.1 ROM) ✅

- [x] `CpcMachine('6128')` in `src/emulator/cpc/` — second 64K RAM, Gate
      Array/PAL RAM-configuration port (`&7Fxx` writes with bits 7–6 = `11`
      select one of the eight standard configurations; the seam was left
      ready in cpc464 Stage 2's `memory.ts`), BASIC 1.1 upper ROM, OS 2.x
      lower ROM, boot-detect against the 6128 boot screen
- [x] ROM into `public/roms/cpc/cpc6128.rom` (one combined 32K image — see the
      target summary) + `ATTRIBUTION.md` extension. Provenance checked: the
      committed `cpc464.rom` is byte-identical to the image the long-running
      CPC emulators ship, so the 6128 was taken from the same source; its
      halves carry CRC32 `0219bb74` (OS 2.x) and `ca6af63d` (BASIC 1.1)
- [x] `gateArray.ts` needed **no change** — cpc464 Stage 2 already forwarded
      `%11xxxxxx` writes to `setRamConfig`, so implementing the eight PAL
      configurations in `memory.ts` was the whole job. Configuration 0 keeps
      the existing direct-RAM path, so the 464 and ordinary 6128 execution are
      untouched by the new indirection
- [x] `createEmulator` in this dialect returns the '6128' model (rom/ramKb
      opts ignored — the machine manages its own ROM set and 128K, like the
      Spectrum 128)
- [x] tests: `cpc6128Boot.test.ts` boots to the `BASIC 1.1` banner, injects and
      RUNs a program, and banks RAM end-to-end through a real `OUT &7F00,&C4`
      from the firmware's own interpreter; `memory.test.ts` covers every
      configuration, bank isolation, the 464's inertness and that the display
      and BASIC-pointer reads stay on the base 64K

**Depends on:** cpc464 Stage 2; Stage A for the injected program bytes.
**Verify:** emulator boot + banking tests pass.

## Stage C — Wire-up: keyboard theme + register ✅

- [x] `keyboardLayout.ts` — re-export the cpc464 layout data with
      `theme: 'vk-theme-cpc6128'` (grey 6128 caps; optional CSS block in
      `src/keyboard/VirtualKeyboard.css`)
- [x] `samples.ts` — re-export the cpc464 samples (they are valid 1.1
      programs); optionally showcase a 1.1 feature (e.g. `FILL` in
      `circles`) in a later polish pass
- [x] `aiProfile.ts` — Locomotive BASIC **1.1** prompt (may use the eleven
      additions; note tape I/O needs `|TAPE`)
- [x] `index.ts` — assemble the `Dialect` (`docsReference: 'cpc'`,
      `programRamBytes` from the shipped boot configuration — see target
      summary)
- [x] **register in `src/dialects/registry.ts` (directly after `cpc464`) and
      add `{ verb: 'fill', dialectId: 'cpc6128' }` to `SHARE_VERBS` in the
      same change** (bijection test)
- [x] `src/dialects/registry.test.ts` — `cpc6128: 'hex'` added to
      `expectedNotation` (it checks every registered dialect)
- [x] **unplanned:** registering the dialect made `machineArtId('cpc6128')`
      fall back to the generic portrait in the machine picker, so a
      `Cpc6128Art` component was drawn (the 464 case with the 3" disc drive in
      place of the cassette deck) and added to `MACHINE_ART_IDS`
- [x] tests: samples tokenize and RUN under 1.1, layout validation reused

**Depends on:** Stages A–B; cpc464 Stage 3 (layout + samples exist).
**Verify:** `npm run typecheck` + `npm test` + `npm run dev` smoke +
`npm run e2e`.

## Stage D — Memory map, introspection & docs deltas ✅

- [x] `memoryMap.ts` — own map: same 64K main layout as cpc464 with
      1.1-workspace labels, plus a note-level treatment of the banked 128K
      (main map stays `addressSpace: 0x10000`; the extra banks are described
      in region notes, the Spectrum 128 precedent)
- [x] `memoryBlocks.ts` — reuse cpc464's figures if HIMEM/defaults match the
      shipped configuration, else own the deltas
- [x] sysvar address table for BASIC 1.1 → `readVariables()` /
      `readReport()` / `readMemoryStats()` / `currentLine()` through the
      variant-keyed readers from cpc464 Stage 5. **Every address was measured
      against the running ROM, and the offsets are not uniform:** the
      program/variable pointer block sits `&1D` below 1.0's, `errCode` `&1A`,
      `errLine` `&22` and `curLinePtr` `&19`. Derive nothing here from a 464
      address. Method: type lines into the booted firmware and watch which four
      words _it_ advances (progEnd/varStart/arrStart/arrEnd = `&AE66`–`&AE6C`);
      diff clean against errored runs for `errCode` `&AD90` / `errLine`
      `&B0A0`; find the word whose deref names the looping line for
      `curLinePtr` `&AE1D`. The same probes reproduce the 464's known addresses
      exactly, which is what validates them
- [x] **unplanned:** `LocoSysVars` gained `progEnd`, and `loadProgram`'s
      injection now takes its four pointers from the model's table instead of
      the hard-coded 464 addresses. This was a real bug, not tidying: a 6128
      given 1.0's addresses never sees the injected program and reports
      "Syntax error in 0" the moment a program declares a variable
- [x] transfer parity: reuse cpc464 `targets.ts`/`audio` with 6128
      `|TAPE`-aware load/save instructions (`.cdt` and WAV are
      format-identical)
- [x] docs delta: the 1.1 tags in `docs/reference/data/cpc.ts` were already
      complete (written during cpc464 Stage 6), so the work was an
      `## Amstrad CPC 6128` section in `docs/reference/cpc/hardware.md` (BASIC
      1.1 keyword summary, the banked second 64K, the tape-only caveat), the
      shared page intro, the reference index and file-formats rows, and the
      roadmap
- [x] follow-up (not this stage): AMSDOS + FDC 765 + `.dsk` import/export;
      revisit `programRamBytes` (42249) and disc build targets when it lands
- [x] tests: memory-map invariants, 1.1 sysvar reads, live-vars parity

**Depends on:** Stage B–C; cpc464 Stages 4–6 for the shared pieces.
**Verify:** memory-map tests; variable watcher live on a 6128 program;
`npm run docs:dev` renders the shared CPC page with 1.1 tags.

---

## Shipped

All four stages landed together. Quality gates: `npm run typecheck`,
`npm test`, `npm run lint`, `npm run format:check`, `npm run docs:build`,
`npm run e2e`.

**Still open (deliberately):** AMSDOS + the µPD765 FDC + `.dsk` import/export.
When that lands, revisit `programRamBytes` (42249 with AMSDOS fitted), add the
disc build targets, model the `&DFxx` upper-ROM select so ROM 7 can answer, and
drop the `|TAPE` preamble from the load/save instructions.
