import type { SampleFile } from '../types';

/**
 * Stub — populated in Stage 3 of docs/dialect-plans/trs80.md with the canonical
 * hello / circles / breakout / maze set ported to Level II BASIC (hello is the
 * starter shown for a fresh document).
 */
export const trs80Samples: SampleFile[] = [];
