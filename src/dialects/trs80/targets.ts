import type { BuildTarget } from '../types';

/**
 * Stub — populated in Stage 4 of docs/dialect-plans/trs80.md with the native
 * `.cas` cassette export and a cassette `.wav` target (via src/transfer/wav.ts).
 */
export const trs80BuildTargets: BuildTarget[] = [];
