10 REM BREAKOUT
20 BORDER 0: PAPER 0: INK 7: CLS
30 PRINT AT 3,12; INK 6; BRIGHT 1; FLASH 1;"BREAKOUT"
40 PRINT AT 8,6; INK 5;"1. KEYBOARD CONTROLS"
50 PRINT AT 10,6; INK 7;"2. KEMPSTON JOYSTICK"
60 PRINT AT 12,6; INK 7;"3. SINCLAIR JOYSTICK (SJS)"
70 FOR n=1 TO 2: BEEP .12,0: BEEP .12,7: BEEP .12,12: NEXT n: BEEP .25,16
80 LET m=1
90 LET k$=INKEY$
100 IF k$="" THEN GO TO 90
110 IF k$="2" THEN LET m=2
120 IF k$="3" THEN LET m=3
130 BORDER 0: PAPER 0: INK 7: CLS
140 LET x=15: LET bx=15: LET by=5: LET dx=1: LET dy=-1: LET s=0
150 FOR i=0 TO 31
160 PRINT AT 1,i; INK 6;"▀"
170 NEXT i
180 PRINT AT 20,x; INK 5;"▀▀▀▀"
182 IF m=1 THEN PRINT AT 0,0;"5 8 TO MOVE"
184 IF m=2 THEN PRINT AT 0,0;"KEMPSTON JOYSTICK"
186 IF m=3 THEN PRINT AT 0,0;"SINCLAIR JOYSTICK"
190 REM GAME LOOP
200 PRINT AT by,bx; INK 2;"O"
210 PAUSE 2
220 LET k$=INKEY$: LET lf=0: LET rt=0
230 IF m=1 AND k$="5" THEN LET lf=1
240 IF m=1 AND k$="8" THEN LET rt=1
250 IF m=3 AND k$="1" THEN LET lf=1
260 IF m=3 AND k$="2" THEN LET rt=1
270 IF m=2 THEN LET j=IN 31: LET rt=j-2*INT (j/2): LET lf=INT (j/2)-2*INT (j/4)
280 IF lf=1 AND x>0 THEN LET x=x-2
290 IF rt=1 AND x<28 THEN LET x=x+2
300 PRINT AT 20,0;"                                "
310 PRINT AT 20,x; INK 5;"▀▀▀▀"
320 PRINT AT by,bx;" "
330 LET bx=bx+dx: LET by=by+dy
340 IF bx<1 OR bx>30 THEN LET dx=-dx: BEEP .008,-12
350 IF by<2 THEN LET dy=-dy: BEEP .008,-12
360 IF by=2 THEN LET s=s+1: BEEP .008,24
370 IF by<19 THEN GO TO 200
380 IF bx>=x AND bx<=x+3 THEN LET dy=-dy: BEEP .008,-12
390 IF by<21 THEN GO TO 200
400 FOR n=30 TO -30: BEEP .016,n: NEXT n
410 PRINT AT 10,10; FLASH 1;"GAME OVER"
420 PRINT AT 12,10;"SCORE ";s
430 PAUSE 300
