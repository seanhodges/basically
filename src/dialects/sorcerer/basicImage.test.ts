import { describe, it } from 'vitest';

describe('sorcerer BASIC image', () => {
  it.todo('writes the program at the interpreter’s program base');
  it.todo('derives control-area pointers that agree with the image');
});
