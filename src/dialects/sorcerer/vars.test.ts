import { describe, it } from 'vitest';

describe('sorcerer variables', () => {
  it.todo('reads scalars and arrays out of the interpreter’s stores');
  it.todo('locates its pointers in the BASIC control area');
});
