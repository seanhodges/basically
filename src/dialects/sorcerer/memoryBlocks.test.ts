import { describe, it } from 'vitest';

describe('sorcerer memory blocks', () => {
  it.todo('keeps the default address clear of BASIC and the Monitor workarea');
  it.todo('rejects a block that would land in ROM or in screen RAM');
});
