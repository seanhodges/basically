import { describe, it } from 'vitest';

describe('sorcerer keywords', () => {
  it.todo('gives every reserved word the token byte the ROM PAC stores');
  it.todo('covers the reserved-word table with no gaps and no extras');
  it.todo('carries a signature and a doc for every entry');
});
