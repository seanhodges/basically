import { describe, it } from 'vitest';

describe('sorcerer memory map', () => {
  it.todo('tiles the whole 64K address space');
  it.todo('names the screen RAM, both generator halves and the ROM PAC');
  it.todo('agrees with memoryBlocks.programArea on the program region');
});
