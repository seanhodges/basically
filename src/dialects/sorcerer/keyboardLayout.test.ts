import { describe, it } from 'vitest';

describe('sorcerer keyboard layout', () => {
  it.todo('reaches every emulator key token by keycap or host key');
  it.todo('reaches the graphics characters through SHIFT + GRAPHIC');
  it.todo('takes its geometry from the shared template rows');
});
