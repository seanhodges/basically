// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2026 Sean Hodges

import { describe, it } from 'vitest';

/** Stage 3 fills these in - see the `authoring-dialect-samples` skill. */
describe('altair8800 samples', () => {
  it.todo('ships the canonical set in the canonical order');
  it.todo('tokenizes every sample without errors');
  it.todo('uses only keywords Altair 8K BASIC actually has');
  it.todo('avoids keyword-as-variable collisions under crunching');
  it.todo('keeps every PRINT inside the terminal width');
});
