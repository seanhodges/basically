import type { BuildTarget } from '../types';

/**
 * Hardware-export targets: AMSDOS-headered `.bas`, `.cdt` tape image and
 * cassette `.wav` (CPC firmware tape scheme). Stage 4 fills this in; empty
 * placeholder so the unregistered dialect stub stays loadable.
 */
export const cpc464BuildTargets: BuildTarget[] = [];
