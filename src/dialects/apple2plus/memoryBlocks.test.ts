import { describe, it } from 'vitest';

describe('apple2plus memory blocks', () => {
  it.todo('offers only RAM the ROM and the interpreter leave alone');
  it.todo('reports the program area the map agrees with');
});
