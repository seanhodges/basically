import { describe, it } from 'vitest';

describe('apple2plus image builder', () => {
  it.todo('builds an image whose workspace pointers agree with the program');
  it.todo('parses its own image back to the program it was built from');
});
