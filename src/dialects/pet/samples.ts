import type { SampleFile } from '../types';

/**
 * TODO (pet Stage 3): canonical hello / circles / breakout / maze ported to
 * monochrome 40×25 PET BASIC (hello is the starter; circles degrades to
 * PETSCII character plotting). See docs/contributing/dialect-plans/pet.md.
 */
export const petSamples: SampleFile[] = [];
