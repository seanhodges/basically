import { describe, it } from 'vitest';

describe('samcoupe machine', () => {
  it.todo('boots the ROM to the SAM BASIC prompt');
  it.todo('runs an injected program and shows its output');
  it.todo('tokenizes a listing at the keyboard identically to tokenize()');
});
