The canonical BASIC-G sample programs land here in Stage 3 — `hello.bas`,
`circles.bas`, `breakout.bas`, `maze.bas`, `kaleido.bas`, registered in that
order by `../samples.ts`. Write them with the `authoring-dialect-samples` skill;
never point the dialect at another machine's `.bas`.
