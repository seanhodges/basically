// The 128K key matrix (and its port-0xFE scan) is identical to the 48K
// Spectrum's, so the keyboard is reused verbatim. See docs/dialect-plans/zxspectrum128.md.
export { SpectrumKeyboard } from '../../zxspectrum/emulator/keyboard';
