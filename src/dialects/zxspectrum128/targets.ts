import type { BuildTarget } from '../types';

/**
 * Stub — populated in Stage 4 of docs/dialect-plans/zxspectrum128.md with the
 * .TAP export and a cassette .wav target (reusing ../zxspectrum/audio's
 * encoder and src/transfer/wav.ts), built from the 128 tokenizer so
 * PLAY/SPECTRUM programs export correctly.
 */
export const spectrum128BuildTargets: BuildTarget[] = [];
