import { describe, it } from 'vitest';

describe('apple2 tokenizer', () => {
  it.todo('round-trips every construct through the detokenizer');
  it.todo('reports an unparseable line without throwing');
});
