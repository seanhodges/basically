import { describe, it } from 'vitest';

describe('apple2 keywords', () => {
  it.todo('carries the token byte the interpreter stores for each keyword');
  it.todo('spells every keyword the way the ROM does');
});
