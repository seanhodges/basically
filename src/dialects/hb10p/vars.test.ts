import { describe, it } from 'vitest';

describe('hb10p variable reader', () => {
  it.todo('walks scalars and arrays from the interpreter’s own pointers');
  it.todo('reads all four value types, including double precision');
});
