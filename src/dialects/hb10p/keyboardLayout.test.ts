import { describe, it } from 'vitest';

describe('hb10p keyboard layout', () => {
  it.todo('reaches every matrix token from a keycap or a host key');
  it.todo('offers the graphics palette entries the charset maps');
  it.todo('presses nothing but the matrix from the function key strip');
});
