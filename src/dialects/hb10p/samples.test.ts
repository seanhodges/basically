import { describe, it } from 'vitest';

describe('hb10p samples', () => {
  it.todo('ships the canonical set in the canonical order');
  it.todo('runs each sample on the real ROM and checks what it draws');
});
