import { describe, it } from 'vitest';

describe('hb10p memory map', () => {
  it.todo('tiles the whole 64K address space');
  it.todo('names the BIOS, BASIC ROM, program area and system variables');
  it.todo('agrees with memoryBlocks about where the program area starts');
});
