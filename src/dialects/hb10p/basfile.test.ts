import { describe, it } from 'vitest';

describe('hb10p .bas container', () => {
  it.todo('writes the 0xFF tokenized marker ahead of the program');
  it.todo('relinks the line pointers to the program base on import');
  it.todo('rejects an ASCII listing rather than reading it as tokens');
});
