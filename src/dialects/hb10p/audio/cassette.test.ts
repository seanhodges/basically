import { describe, it } from 'vitest';

describe('hb10p cassette', () => {
  it.todo('encodes a 0 bit as one 1200Hz cycle and a 1 as two at 2400Hz');
  it.todo('round-trips a program through encode and decode');
});
