/**
 * Atom BASIC detokenizer. STUB — Stage 1 implements Atom program image → text
 * (the inverse of {@link tokenizeProgram}).
 */
export function detokenizeProgram(_image: Uint8Array): string {
  throw new Error('atom: detokenizer not implemented');
}
