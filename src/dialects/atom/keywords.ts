import type { KeywordInfo } from '../types';

/**
 * Acorn Atom BASIC keyword table. STUB — Stage 1 fills this with the genuine
 * Atom BASIC keywords and token bytes. Atom BASIC shares none of the BBC token
 * layout, so this must be built from scratch (do not import from bbcmicro).
 */
export const atomKeywords: KeywordInfo[] = [];
