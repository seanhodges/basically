import type { BuildTarget } from '../types';

/**
 * Atom build targets (native binary export + cassette `.wav`). STUB — Stage 4
 * fills this in. Empty until then so the (unregistered) dialect offers no
 * exports yet.
 */
export const atomBuildTargets: BuildTarget[] = [];
