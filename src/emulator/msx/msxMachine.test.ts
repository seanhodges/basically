import { describe, it } from 'vitest';

describe('MsxMachine', () => {
  it.todo('boots the system ROM to the MSX BASIC sign-on');
  it.todo('reports the machine’s free-memory figure on the banner');
  it.todo('renders SCREEN 1 text out of the VDP name table');
  it.todo('advances identically over a window of frames and of debug slices');
});
