import { describe, it } from 'vitest';

describe('Apple1Machine', () => {
  it.todo('boots the monitor and reaches its prompt');
  it.todo('cold-starts Integer BASIC and reaches its prompt');
  it.todo('reports a monitor-only image rather than failing to start');
});
